{
  "name": "erikainnaco-website",
  "version": "1.0.0",
  "scripts": {
    "build:css": "tailwindcss -i ./assets/css/main.css -o ./assets/css/styles.css --minify",
    "watch:css": "tailwindcss -i ./assets/css/main.css -o ./assets/css/styles.css --watch",
    "dev": "npm run build:css && concurrently \"bundle exec jekyll serve --livereload\" \"npm run watch:css\"",
    "build": "npm run build:css && JEKYLL_ENV=production bundle exec jekyll build"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "tailwindcss": "^3.4.0"
  }
}
